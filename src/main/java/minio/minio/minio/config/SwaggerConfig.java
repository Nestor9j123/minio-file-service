package minio.minio.minio.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Configuration simplifiée - utilise les defaults SpringDoc
}
